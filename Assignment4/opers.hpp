// the header of operators

#ifndef OPERS
#define OPERS

#include "Transformer.hpp"

bool operator<=(const Transformer& t1, const Transformer& t2);
bool operator>=(const Transformer& t1, const Transformer& t2);
bool operator>(const Transformer& t1, const Transformer& t2);

#endif
